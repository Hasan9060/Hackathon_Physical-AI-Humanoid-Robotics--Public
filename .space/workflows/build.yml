# Build workflow for Hugging Face Spaces
# This file configures how your Space is built and deployed

# Build the Docusaurus site
build:
  - npm install
  - npm run build

# Serve the built site
port: 3000

# Specify the build output directory
build_output_dir: "build"
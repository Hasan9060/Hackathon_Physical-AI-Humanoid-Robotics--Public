{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": {
        "node": "20"
      }
    }
  },
  "deploy": {
    "startCommand": "npm run build && npm run serve",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}